# IMPORTANT: Do NOT commit this file with real secret values.
# This file is a template only — replace placeholder values before applying.
#
# Recommended secret management approaches:
#   - Sealed Secrets  (https://github.com/bitnami-labs/sealed-secrets)
#   - External Secrets Operator  (https://external-secrets.io)
#   - HashiCorp Vault Agent Injector
#
# Apply after substituting real values:
#   kubectl apply -f k8s/secret.yaml
#
# Or use envsubst in CI/CD:
#   envsubst < k8s/secret.yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: chatbot-secrets
  namespace: k8s-rag-chatbot
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_WITH_REAL_KEY"
  # ANTHROPIC_API_KEY: "REPLACE_WITH_REAL_KEY"  # Uncomment for Claude fallback
